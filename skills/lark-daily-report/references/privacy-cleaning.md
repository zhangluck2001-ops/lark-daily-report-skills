# 工作相关性判定

默认由 AI 判断素材是否适合写入日报，不要只按关键词清洗。

注意：`inspect-sources.mjs --index` 会用轻量候选信号给会话排序，以节省 token；这一步不是最终工作相关性判断。被省略会话仍会输出无正文 metadata，必要时按 `--chat-id` 展开后再判断。

保留：今天的工作进展、交付、决策、待办、阻塞、必要协同和可验证结论。

丢弃：与工作无关的聊天、个人隐私、系统通知、广告、人事考勤噪声、敏感凭据、敏感个人身份/财务信息和无法支撑工作事实的内容。

写入日报时只写归纳后的工作事实，不复制聊天原文，不展示清洗过程。
